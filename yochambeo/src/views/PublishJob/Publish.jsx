import { PublishJob } from '../../components/publishJob'; // Asegúrate de que la ruta es correcta

export const PublishJobView = () => {
    return (
        <div>
            <PublishJob />
        </div>
    );
};
